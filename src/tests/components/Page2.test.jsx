import React from 'react';
import ReactDOM from 'react-dom';
import expect from 'expect';
import $ from 'jQuery';
import TestUtils from 'react-addons-test-utils';

import Page2 from 'Page2';

describe('Page2', () => {
  it ('should exist', () => {
    expect(Page2).toExist();
  });
});
